"use client"; import Link from "next/link"; import ProductGrid from "@/components/ProductGrid"; import { useShopStore } from "@/store/useShopStore"; import { products } from "@/data/products";
export default function Wishlist(){const ids=useShopStore(s=>s.wishlist); const items=products.filter(p=>ids.includes(p.id)); return <main className="container py-12"><p className="text-xs tracking-[.3em] text-burgundy">SAVED FOR LATER</p><h1 className="serif text-5xl mt-2">Wishlist</h1>{items.length?<div className="mt-10"><ProductGrid products={items}/></div>:<div className="py-24 text-center"><h2 className="serif text-3xl">Your wishlist is waiting for its first favorite ❤️</h2><Link href="/shop" className="inline-block bg-plum text-white px-6 py-3 mt-7 text-xs tracking-widest">EXPLORE COLLECTION</Link></div>}</main>}