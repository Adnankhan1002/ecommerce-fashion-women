"use client"; import Link from "next/link"; import { useShopStore } from "@/store/useShopStore"; import { Package, Sparkles, Heart } from "lucide-react";
export default function Account(){const {orders,coins,wishlist}=useShopStore(); return <main className="container py-12"><p className="text-xs tracking-[.3em] text-burgundy">MY AURELIA</p><h1 className="serif text-5xl mt-2">Welcome back.</h1><div className="grid md:grid-cols-3 gap-4 mt-10"><div className="bg-white p-6"><Sparkles className="text-burgundy"/><div className="text-3xl mt-5">{coins}</div><p className="text-xs text-black/45">Magic Coins</p><Link href="/magic-coins" className="text-xs text-burgundy mt-5 inline-block">VIEW REWARDS →</Link></div><div className="bg-white p-6"><Package className="text-burgundy"/><div className="text-3xl mt-5">{orders.length}</div><p className="text-xs text-black/45">Orders</p></div><div className="bg-white p-6"><Heart className="text-burgundy"/><div className="text-3xl mt-5">{wishlist.length}</div><p className="text-xs text-black/45">Wishlist items</p></div></div><section className="mt-12 bg-white p-6"><h2 className="serif text-3xl">Recent orders</h2>{orders.length?<div className="mt-5 divide-y">{orders.slice(0,5).map(o=><div key={o.id} className="py-4 flex justify-between"><span>{o.id}<span className="block text-xs text-black/40 mt-1">{new Date(o.createdAt).toLocaleDateString()}</span></span><span>₹{o.total.toLocaleString("en-IN")}</span></div>)}</div>:<p className="text-sm text-black/45 mt-4">No orders yet. Your next favorite look is waiting.</p>}</section></main>}