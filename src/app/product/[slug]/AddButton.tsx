"use client"; import { useState } from "react"; import { useShopStore } from "@/store/useShopStore"; import { Product } from "@/types"; import { Check } from "lucide-react"; import Link from "next/link";
export default function AddButton({product}:{product:Product}){const add=useShopStore(s=>s.addToCart); const [done,setDone]=useState(false); return <div className="mt-8 grid grid-cols-2 gap-3"><button onClick={()=>{add({product,quantity:1,size:"M",color:product.colors[0]});setDone(true);setTimeout(()=>setDone(false),1800)}} className="bg-plum text-white py-4 tracking-widest text-xs">{done?<span className="flex justify-center gap-2"><Check size={15}/> ADDED</span>:"ADD TO CART"}</button><Link href="/cart" className="border border-plum text-plum py-4 text-center tracking-widest text-xs">BUY NOW</Link></div>}